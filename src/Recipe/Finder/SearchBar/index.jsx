import React from "react";

import { Bar, Input, Button } from "./Styles";

const SearchBar = () => {
  return (
    <Bar>
      <Input />
      <Button>GO</Button>
    </Bar>
  );
};

export default SearchBar;